import 'package:flutter/material.dart';

class Page_dangnhap extends StatelessWidget {
  const Page_dangnhap({super.key});

  @override
  Widget build(BuildContext context) {
    return SafeArea(
        child: Scaffold(
      appBar: AppBar(title: Text("dang nhập")),
    ));
  }
}
